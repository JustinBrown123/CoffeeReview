using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace CoffeeReview.Repositories
{
    public class ReviewRepository
    {

        // Create a Review Repository Class

        //- Should be configured so it will be injected into the ReviewsController;

        //-Should have a constructor that houses a dictionary to store your reviews, using each review's ID as the key;

        //- All review data should be hard coded in the Dictionary;

        //- It should have a method to get all reviews;

        //- It should have a method to find one review by ID.

    }
}
