@model IEnumerable<CoffeeReview.Models.Coffee>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers


@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>


    <h3>Choose a Coffee To Delete</h3>
    <h2>Coffees</h2>
    <div id="coffees">
        <form action="/Coffee/DeleteById/" method="post">

            <select name="Id">
                @foreach (var coffee in Model)
                {


                    <option value="@coffee.Id">@coffee.Brand, @coffee.Kind</option>

                }
            </select>
            <input type="submit" value="Delete a Coffee" class="btn btn-default" />
        </form>




    </div>
    <div>
        @*<h4>Coffee</h4>
        <hr />
        <dl class="dl-horizontal">
            <div class="form-group">
                <label asp-for="Kind" class="control-label"></label>
                <input asp-for="Kind" class="form-control" />
                <span asp-validation-for="Kind" class="text-danger"></span>
            </div>
            <dt>
                @Html.DisplayNameFor(model => model.Brand)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Brand)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Kind)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Kind)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ImageUrl)
            </dd>
        </dl>


        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index">Back to List</a>
        </form>*@
    </div>
</body>
</html>
